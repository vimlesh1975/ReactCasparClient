import React from 'react'
import { gsap } from 'gsap';
window.gsap = gsap;
window.tl = gsap.timeline();

const CanvasPlayer = () => {
    return (
        <div></div>
    )
}

export default CanvasPlayer